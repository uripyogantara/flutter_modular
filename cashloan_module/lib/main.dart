import 'package:flutter/material.dart';

class CashloanPage extends StatelessWidget {
  static const String PATH = '/cashloan';
  static const String TAG = 'Cashloan';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Modul Kasbon"),
      ),
    );
  }
}
